type: object
required:
    - firstName
    - lastName
    - email
    - phoneNumber
properties:
    firstName:
        type: string
        minLength: 2
        maxLength: 32
        description: Client first name. Latin letters only.
        example: "John"
    lastName:
        type: string
        minLength: 2
        maxLength: 32
        description: Client last name. Latin letters only.
        example: "Doe"
    email:
        type: string
        format: email
        description: Client email address
        example: "john.doe@mail.com"
    phoneNumber:
        type: string
        description: Client phone number
        example: "+37101234567"
